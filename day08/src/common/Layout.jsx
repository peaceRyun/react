import styled from 'styled-components';

const MainCon = styled.main``;

const Layout = () => {
    return (
        <div>
            <MainCon></MainCon>
        </div>
    );
};

export default Layout;
